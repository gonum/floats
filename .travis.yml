language: go

go:
 - 1.1
 - 1.2
 - 1.3
 - release
 - tip

script:
 - go get -d -v ./... && go build -v ./...
 - go test -v ./...
 - diff <(gofmt -d .) <("") 

after_failure: failure
after_error: failure

notifications:
  email:
    recipients:
      - jonathan.lawlor@gmail.com
    on_success: change
    on_failure: always
